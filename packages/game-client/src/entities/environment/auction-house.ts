import { CraftingStationClient } from "./crafting-station";

/** Renders the right 16×16 frame on `locker.png` (32×16 sheet: bank locker | auction stall). */
export class AuctionHouseClient extends CraftingStationClient {}
