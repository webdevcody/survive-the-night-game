/**
 * ========================================================================
 * META CONFIGURATION
 * ========================================================================
 * Game version and meta information
 */

export const metaConfig = {
  VERSION: "v0.13.0",
} as const;

export type MetaConfig = typeof metaConfig;
