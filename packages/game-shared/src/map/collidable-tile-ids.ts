/**
 * Collidables-layer tile ids referenced by gameplay / rendering.
 * Sprite crop uses the same indexing as the map editor: column = id % sheetCols, row = floor(id / sheetCols).
 */
export const COLLIDABLE_TILE_MERCHANT = 255;
